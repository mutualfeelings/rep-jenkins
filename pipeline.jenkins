pipeline{
    agent any
    stages{
        stage("Test pipeline"){
            steps{
                echo "========executing A========"
                sh "echo $USER"
                sh "docker version"
            }
        }
        stage("Checkout") { 
            steps{
                git branch: "main",
                    url: 'https://github.com/mutualfeelings/rep-jenkins.git'

                sh 'pwd'

            }
        }
    }
    
}